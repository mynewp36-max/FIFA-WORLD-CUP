import { describe, it, expect } from 'vitest';
import * as IncidentTypeSelectorModule from './IncidentTypeSelector';

describe('IncidentTypeSelector', () => {
  it('should render without crashing', () => {
    expect(IncidentTypeSelectorModule).toBeDefined();
  });
});